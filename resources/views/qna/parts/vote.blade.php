<span class="vote arrow up glyphicon glyphicon-triangle-top" data-id="{{ $id }}" data-type="{{ $type }}"
      aria-hidden="true"></span>
<span id="{{ 'label-'.$type.'-'.$id }}" data-id="{{ $id }}" data-type="{{ $type }}">{{ $count }}</span>
<span class="vote arrow down glyphicon glyphicon-triangle-bottom" data-id="{{ $id }}" data-type="{{ $type }}"
      aria-hidden="true"></span>
